@model IEnumerable<ModuloMVC.Models.Contact>

@{
    ViewData["Title"] = "Listagem de contatos";
}

<h2>Contatos</h2>
<p>
    <a asp-action="Create">Novo Contato</a>
</p>

<table class="table" >
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            <th>@Html.DisplayNameFor(model => model.Nome)</th>
            <th>@Html.DisplayNameFor(model => model.Telefone)</th>
            <th>@Html.DisplayNameFor(model => model.Ativo)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var contact in Model)
        {
            <tr>
                <td>@Html.DisplayFor(model => contact.Id)</td>
                <td>@Html.DisplayFor(model => contact.Nome)</td>
                <td>@Html.DisplayFor(model => contact.Telefone)</td>
                <td>@Html.DisplayFor(model => contact.Ativo)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@contact.Id">Editar</a> | 
                    <a asp-action="Details" asp-route-id="@contact.Id">Detalhes</a> | 
                    <a asp-action="Delete" asp-route-id="@contact.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>